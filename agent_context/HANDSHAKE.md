# Agent Handshake

## Current Session Status
- Active Agent: unknown
- Budget Tier applied: `memory_os nano` / score 1
- Target: Initial Memory OS bootstrap.
## 🤖 Environment Initialization (CRITICAL)
Якщо проект було перенесено на нову машину або ти щойно прокинувся в новому оточенні:
**ЗАВЖДИ використовуй `./launch.sh` як єдину точку входу для запуску скриптів.**
Він автоматично створить віртуальне середовище `venv_auto` (якщо його немає) та підтягне всі залежності з `pyproject.toml`. Не використовуй системний глобальний Python.
